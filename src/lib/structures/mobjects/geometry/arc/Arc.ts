import { MObject } from '../../MObject';

export class Arc extends MObject {
  constructor(
    angle?: number,
    width?: number,
    start_angle?: number,
    radius?: number,
    fill_opacity?: number,
    stroke_width?: number,
    num_components?: number,
    arc_center?: number[],
  ) {
    super();
  }
}
